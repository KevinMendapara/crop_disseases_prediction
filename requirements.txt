Flask
numpy
pillow
# The bundled .keras model was saved by Keras 3.15.1.  Pin the matching
# TensorFlow/Keras generation so the deployed service can deserialize it.
tensorflow-cpu==2.20.0
keras==3.15.1
gunicorn; sys_platform != 'win32'
flask-cors


